<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="PsEvent">
    <resultMap id="PsEventResult" type="org.pssframework.model.statistics.PsEvent"></resultMap>
    <!-- 用于select查询公用抽取的列 -->
    <sql id="PsEventColumns">
        <![CDATA[
        e.LBSJ_ID lbsjId, e.GP_ID gpId, p.PS_ID psId, p.PS_NAME psName, p.ASSET_NO assetNo, e.EVENTCODE eventCode, c.EVENTNAME eventName, e.TRIGTIME trigTime, e.RECEIVETIME receiveTime, e.CLOSED closed, e.LOCKED locked, e.PHASE phase, e.CURRENTVALUE currentValue, e.RESETSENT resetSent
        ]]>
    </sql>
    <sql id="PsEventDynamicWhere">
        <!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
        <choose>
            <when test="psId != null"><![CDATA[and p.PS_ID = #{psId}]]></when>
        </choose>
        <choose>
            <when test="orgId != null"><![CDATA[and p.ORG_ID = #{orgId}]]></when>
        </choose>
        <choose>
            <when test="tgId != null and tgId != -1"><![CDATA[and p.OBJECT_ID = #{tgId} and p.GP_TYPE = '2']]></when>
        </choose>
        <choose>
            <when test="ddate != null"><![CDATA[and e.TRIGTIME >= to_date(#{ddate},'yyyy-mm-dd') and e.TRIGTIME < to_date(#{ddate},'yyyy-mm-dd') + 1]]></when>
            <otherwise><![CDATA[and e.TRIGTIME is null]]></otherwise>
        </choose>
    </sql>
    <select id="count" resultType="long">
        <![CDATA[select count(1) from E_LOUBAO_EVENT e, E_LOUBAO_ECODE c, V_PS p where p.GP_ID= e.GP_ID and c.EVENTCODE = e.EVENTCODE]]>
        <include refid="PsEventDynamicWhere" />
    </select>
    <!-- 分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页 因为分页查询将传 #offset#,#pageSize#,#lastRows# 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现 -->
    <select id="pageSelect" resultMap="PsEventResult">
        select
        <include refid="PsEventColumns" />
        <![CDATA[from E_LOUBAO_EVENT e, E_LOUBAO_ECODE c, V_PS p where p.GP_ID= e.GP_ID and c.EVENTCODE = e.EVENTCODE]]>
        <include refid="PsEventDynamicWhere" />
        <if test="sortColumns != null and sortColumns.length() != 0">order by ${sortColumns}</if>
    </select>
</mapper>